export default (theme) => ({
  root: {},
  dropdownButton: {
    marginRight: -theme.spacing.unit * 2,
  },
  chartWrapper: {
    height: "400px",
    position: "relative",
  },
  portletFooter: {
    display: "flex",
    justifyContent: "flex-end",
    color: "white", // Setting content color to white
  },
});
